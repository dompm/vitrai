[project]
name = "vitraux-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "modal>=0.73",
    "python-multipart>=0.0.27",
]
